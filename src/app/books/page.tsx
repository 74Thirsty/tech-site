import { books } from "@/lib/content";
export const metadata={title:"Books — Stratagem"};
export default function BooksPage(){return <main className="subpage"><header className="site-header"><a className="brand" href="/"><span className="brand-mark">N</span><span>STRATAGEM</span></a><a className="text-link" href="/">← Home</a></header><section className="subpage-hero"><div className="card-kicker">THE VAULT / BOOKS + FIELD GUIDES</div><h1>READ<br/><em>DEEPER.</em></h1><p>Long-form guides for people who want to understand the systems beneath the surface.</p></section><section className="books-page-grid">{books.map((book)=><article className="book-feature" key={book.slug}><div className="book-cover"><img src={book.coverImage} alt={book.title} style={{width:"100%",height:"100%",objectFit:"cover"}} /></div><div><div className="card-kicker">{book.author}</div><h2>{book.title}</h2><p>{book.description}</p><div className="book-actions"><a className="button button-primary" href={book.appleBooksUrl} target="_blank" rel="noopener noreferrer">View on Apple Books <span>↗</span></a></div></div></article>)}</section></main>}
