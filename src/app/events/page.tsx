import { events } from "@/lib/content";

export const metadata = {title:"Events — Stratagem"};
export default function EventsPage() { return <main className="subpage"><header className="site-header"><a className="brand" href="/"><img className="brand-mark" src="/favicon.png" alt="Stratagem" width="26" height="26" /><span>STRATAGEM</span></a><a className="text-link" href="/">← Back to arcade</a></header><section className="subpage-hero"><div className="card-kicker">LIVE TRANSMISSIONS / EVENTS</div><h1>UPCOMING<br/><em>MISSIONS.</em></h1><p>Workshops, live sessions, and rooms for technically curious people.</p></section><section className="event-page-list">{events.map((event) => <article className="event-page-card" key={event.title}><b>{new Date(event.date).toLocaleDateString("en-US",{month:"short",day:"numeric"})}</b><div><h2>{event.title}</h2><p>{event.location} / {event.type}</p></div><a className="button button-primary" href="#">Register ↗</a></article>)}</section></main>; }
