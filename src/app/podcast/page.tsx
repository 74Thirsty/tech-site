import episodes from "@/content/podcast.json";
type Episode={number:string;title:string;date:string;duration:string;summary:string;transcript?:string};
const episodeList=episodes as Episode[];
export const metadata={title:"Podcast — Stratagem"};
export default function PodcastPage(){return <main className="subpage"><header className="site-header"><a className="brand" href="/"><img className="brand-mark" src="/favicon.png" alt="Stratagem" width="26" height="26" /><span>STRATAGEM</span></a><nav className="desktop-nav"><a href="/newsletter">NEWSLETTER</a><a href="/podcast">PODCAST</a></nav><a className="text-link" href="/">← Home</a></header><section className="subpage-hero"><div className="card-kicker">SIGNAL ROOM / PODCAST</div><h1>LONG FORM<br/><em>SIGNAL.</em></h1><p>Conversations about systems, infrastructure, and building for the long game.</p></section>{episodeList.length===0?<section className="vault-page-list"><div style={{textAlign:"center",padding:"80px 0"}}><div className="card-kicker">FORTHCOMING</div><h2 style={{fontSize:"clamp(36px,5vw,60px)",fontWeight:500,letterSpacing:"-.07em",margin:"20px 0"}}>EPISODES<br/><em>COMING SOON.</em></h2><p style={{color:"var(--muted)",maxWidth:400,margin:"0 auto",lineHeight:1.5}}>Long-form conversations about systems, security, and building for the long game. The signal room opens soon.</p></div></section>:<section className="vault-page-list">{episodeList.map((episode)=><article className="note" id={`ep-${episode.number}`} key={episode.number}><span className="note-date">EP {episode.number} / {episode.date}</span><strong>{episode.title}</strong><p style={{color:"var(--muted)",marginTop:12,lineHeight:1.5}}>{episode.summary}</p><span className="note-arrow">{episode.duration}</span></article>)}</section>}</main>}
