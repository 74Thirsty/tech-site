import { projects } from "@/lib/content";
import type { ReactNode } from "react";
export const metadata={title:"Projects — NEON//FORGE"};
export default function ProjectsPage(){return <SubPage><div className="subpage-hero"><div className="card-kicker">PROJECT DATABASE / SYSTEMS IN THE OPEN</div><h1>THE<br/><em>WAR ROOM.</em></h1><p>Architecture, experiments, and products built with intent. Open a system to inspect how it works.</p></div><section className="project-page-list">{projects.map((project,index)=><article className="project-detail" id={project.name.toLowerCase().replaceAll(" ","-")} key={project.name}><header><div><div className="card-kicker">0{index+1} / {project.type}</div><h2>{project.name}</h2></div><span className="status-dot">● {project.status}</span></header><p className="hero-lede">{project.description}</p><div className="architecture">{project.steps.map((step)=><span key={step}>{step}</span>)}</div><div className="tags">{project.stack.map((item)=><span key={item}>{item}</span>)}</div></article>)}</section></SubPage>}
function SubPage({children}:{children:ReactNode}){return <main className="subpage"><header className="site-header"><a className="brand" href="/"><span className="brand-mark">N</span><span>NEON<span className="brand-slash">//</span>FORGE</span></a><nav className="desktop-nav"><a href="/projects">PROJECTS</a><a href="/newsletter">SIGNAL</a><a href="/books">BOOKS</a></nav><a className="text-link" href="/">← Home</a></header>{children}</main>}
