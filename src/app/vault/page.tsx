import { articles } from "@/lib/content";
export const metadata={title:"Vault — NEON//FORGE"};
export default function VaultPage(){return <main className="subpage"><header className="site-header"><a className="brand" href="/"><span className="brand-mark">N</span><span>NEON<span className="brand-slash">//</span>FORGE</span></a><a className="text-link" href="/">← Home</a></header><section className="subpage-hero"><div className="card-kicker">KNOWLEDGE DATABASE / FIELD NOTES</div><h1>THE<br/><em>VAULT.</em></h1><p>Technical writing, missions, and useful rabbit holes from the archive.</p></section><section className="vault-page-list">{articles.map((article)=><a className="note" href={`/vault/${article.slug}`} key={article.slug}><span className="note-date">{article.category} / {article.difficulty}</span><strong>{article.title}</strong><span className="note-arrow">+{article.xp} XP ↗</span></a>)}</section></main>}
