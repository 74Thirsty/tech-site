import type { Collector } from "@/intelligence/types";
export const githubCollector:Collector = {name:"GITHUB",async collect(){const response=await fetch("https://api.github.com/search/repositories?q=stars:%3E1000+pushed:%3E2026-07-01&sort=stars&order=desc",{headers:{Accept:"application/vnd.github+json","User-Agent":"neon-forge-research"},next:{revalidate:900}});if(!response.ok)throw new Error(`GitHub ${response.status}`);const data=await response.json();return (data.items??[]).slice(0,10).map((item:{id:number;name:string;html_url:string;description:string|null;topics?:string[]})=>({id:`github-${item.id}`,title:item.name,url:item.html_url,source:"GITHUB",summary:item.description??"Trending open-source project.",topics:item.topics??["PROGRAMMING"],publishedAt:new Date().toISOString()}));}};
